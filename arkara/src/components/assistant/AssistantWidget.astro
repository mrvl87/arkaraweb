---
import type { AssistantMode } from "../../lib/assistant/contracts";

const apiBaseUrl = import.meta.env.PUBLIC_ASSISTANT_API_URL ?? "https://arkaraaiassistant-production.up.railway.app";
const {
  initialMode,
  context
}: {
  initialMode?: AssistantMode;
  context?: {
    url?: string;
    title?: string;
    pathname?: string;
    description?: string;
    bodyText?: string;
  };
} = Astro.props;
---

<arkara-assistant-widget
  api-base-url={apiBaseUrl}
  assistant-page-url="/assistant"
  site-name="Arkara"
  initial-mode={initialMode}
  context-url={context?.url}
  context-title={context?.title}
  context-pathname={context?.pathname}
  context-description={context?.description}
  context-body-text={context?.bodyText}
></arkara-assistant-widget>

<script>
  import "../../lib/assistant/widget";
</script>
